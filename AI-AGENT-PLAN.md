# AI 婚礼策划助手 - 开发计划

## 整体架构

```
用户输入 → 前端聊天界面 → POST /api/agent/chat → Agent 主循环
                                                    ↓
                                              千问 LLM (qwen3.8-27b)
                                                    ↓
                                          判断：需要查数据？
                                            ↙        ↘
                                          是           否
                                          ↓            ↓
                                    调用工具查数据库    直接回复
                                          ↓
                                    拿到结果返回 LLM
                                          ↓
                                    LLM 组织语言回复用户
```

---

## 已完成的工作

### ✅ 1. 后端 Agent 核心（我写的）

| 文件 | 作用 | 状态 |
|------|------|------|
| `src/agent/tools.js` | 定义 4 个工具的 schema（搜场地、搜摄影师、搜花店、算预算） | ✅ |
| `src/agent/tool-executor.js` | 工具执行器，调用数据库查询 | ✅ |
| `src/agent/prompt.js` | System Prompt（领域知识 + 对话策略） | ✅ |
| `src/agent/index.js` | ReAct 主循环（调千问 API） | ✅ |
| `src/routes/agent.js` | REST API 路由 | ✅ |
| `src/index.js` | 路由注册 | ✅ |
| `.env` | API Key 配置 | ✅ |

### ✅ 2. 基础功能验证

- 第一轮对话：Agent 成功追问目的地和预算 ✅
- API 正常返回 JSON ✅

---

## 待完成的工作

### ⬜ 3. 测试工具调用（你需要做）

**目的**：验证 Agent 能否正确查询数据库并推荐真实场地

**操作**：
```bash
# 先启动后端
cd Verra-Voile-End
npm run dev

# 发一条包含具体信息的消息（用你上次的 sessionId）
curl -X POST http://localhost:3000/api/agent/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "意大利吧，预算15万，60人", "sessionId": "你的sessionId"}'
```

**观察**：
- 后端终端应该打印 `[Agent] 调用工具: search_venues(...)` 等日志
- 返回的 reply 应该包含你数据库中真实的意大利场地名称

**判断标准**：
- ✅ 日志显示工具被调用 + 返回了真实场地 → 通过
- ❌ 没有工具调用日志 / 编造了不存在的场地 → 需要排查

---

### ⬜ 4. 测试多轮对话（你需要做）

**目的**：验证 Agent 是否记住上下文

**操作**：
```bash
# 第三轮：修改需求
curl -X POST http://localhost:3000/api/agent/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "摄影师有什么推荐？", "sessionId": "同一个sessionId"}'
```

**判断标准**：
- ✅ Agent 知道你在说意大利的摄影师 → 通过
- ❌ Agent 问你"请问您想去哪个国家" → 上下文丢失，需要排查

---

### ⬜ 5. 调优 Prompt（我来做，你反馈）

**依赖**：步骤 3、4 的测试结果

**你需要的反馈**：
- Agent 回复的语气是否合适？（太正式？太随意？）
- 推荐的方案是否合理？
- 有没有编造数据？
- 追问是否太多/太少？

**我来改**：根据反馈修改 `src/agent/prompt.js`

---

### ⬜ 6. 前端聊天界面（我来写）

**我会创建**：
- `src/pages/AgentChat.tsx` — AI 聊天页面
- 包含：消息列表、输入框、发送按钮、加载状态
- 样式与现有项目风格一致

**你需要做的**：无，我来完成

---

### ⬜ 7. 前端路由注册（我来做）

**我会修改**：
- `src/App.tsx` — 添加 `/agent-chat` 路由
- 可能在首页或导航栏加入口链接

---

### ⬜ 8. 部署后端（你需要做 / 我来协助）

**你需要做的**：
- 确认服务器上的 `.env` 有 `DASHSCOPE_API_KEY`
- 运行部署命令

**我会提供**：部署命令和步骤

---

### ⬜ 9. 部署前端（你需要做 / 我来协助）

---

### ⬜ 10. 线上测试（你需要做）

---

## 你的工作清单（按顺序）

```
□ 1. 测试工具调用（步骤 3）→ 告诉我结果
□ 2. 测试多轮对话（步骤 4）→ 告诉我结果
□ 3. 体验 Agent 回复质量（步骤 5）→ 给我反馈
□ 4. 部署后端到服务器（步骤 8）→ 我提供命令
□ 5. 线上端到端测试（步骤 10）→ 确认一切正常
```

## 我的工作清单

```
✅ 后端代码全部写完
□ 根据测试结果调优 Prompt（等你反馈）
□ 写前端聊天界面（等你确认后端测试通过）
□ 写前端路由注册
□ 提供部署命令
```
